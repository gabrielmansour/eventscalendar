%h1 Creating venue

= error_messages_for :venue

= form_for(:venue, :url => objects_url) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"

= link_to 'Back', objects_path
