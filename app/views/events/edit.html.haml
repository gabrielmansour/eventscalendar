%h1 Editing event

!= error_messages_for :event

- form_for :event, :url => object_url, :method => :put do |f|
  != render "form", :f => f
  %p!= submit_tag "Update"

!= link_to 'Show', object_path
|
!= link_to 'Back', objects_path
